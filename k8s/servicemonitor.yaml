apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: fog-monitor
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: fog-app
  endpoints:
  - port: http
    path: /metrics
    interval: 10s