apiVersion: v1
kind: Service
metadata:
  name: fog-service
  labels:
    app: fog-app
spec:
  type: NodePort
  selector:
    app: fog-app
  ports:
    - name: http
      port: 80
      targetPort: 8000
      nodePort: 30007